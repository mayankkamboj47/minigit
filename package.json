{
    "devDependencies": {
        "@types/node": "^20.5.4",
        "jest": "^29.6.3",
        "mock-fs": "^5.2.0",
        "typescript": "^5.1.6"
    },
    "scripts": {
        "test": "jest",
        "compile": "tsc",
        "ct": "npm run compile && npm run test"
    },
    "dependencies": {
        "glob-promise": "^6.0.3"
    }
}
